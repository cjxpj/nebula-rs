# $主回调$ — 匹配主词条

基础函数 | 无需导入。

<dl>
  <dt>格式</dt><dd><code>$主回调 [触发词]$</code></dd>
  <dt>参数</dt><dd>触发词</dd>
  <dt>返回值</dt><dd>匹配到的词条的执行结果</dd>
</dl>

以指定触发词匹配普通词条，在**当前变量上下文**中执行。

```
$主回调 你好$          ← 匹配主词条"你好"
```

与 `$回调$` 的区别：`$回调$` 匹配内部词条（新上下文），`$主回调$` 匹配普通词条（保留上下文）。

> `$回调$` 和 `$主回调$` 是 NR 实现逻辑分发和控制反转的核心。