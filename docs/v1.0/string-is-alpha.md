# $判断字母$ — 判断是否全为字母

导入：`#引入=@字符串` | 标准库函数。

<dl>
  <dt>签名</dt><dd><code>$判断字母 [字符串]$</code></dd>
  <dt>参数</dt><dd>一个字符串</dd>
  <dt>返回值</dt><dd>"1"（全为 A-Z、a-z）或 "0"（否则）</dd>
</dl>

```
$判断字母 Hello$        → "1"
```