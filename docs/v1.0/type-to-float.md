# $t.转浮点$ — 文本转浮点

导入：`t:#引入=@类型` | 标准库函数。

- **签名**：`$t.转浮点 [文本]$`
- **参数**：要转换的文本
- **返回值**：浮点数值；失败时返回空字符串

将文本解析为 f64 浮点数，与 `$t.转数字$` 行为一致。

```
$t.转浮点 3.14$        → "__N3.14"
$t.转浮点 5$           → "__N5"
```