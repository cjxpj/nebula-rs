# $net.新建$ — 创建请求对象

导入：`net:#引入=@访问` | 标准库函数。

- **签名**：`$net.新建 [url]$`
- **参数**：目标 URL
- **返回值**：请求句柄

初始化 HTTP 请求状态机，默认 GET 方法。每个请求句柄代表一个独立的 HTTP 请求状态机。

```
req:$net.新建 https://httpbin.org/post$
```