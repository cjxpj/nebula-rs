# $存在文件$ — 判断文件是否存在

基础函数 | `#引入=@文件`

<dl>
  <dt>签名</dt><dd><code>$存在文件 [路径]$</code></dd>
  <dt>参数</dt><dd>文件路径</dd>
  <dt>返回值</dt><dd>"true"（存在）或 "false"（不存在）</dd>
</dl>

判断指定路径的文件是否存在（不包含目录）。

```
$存在文件 config.yaml$
```

> 建议在关键文件操作前使用此函数做前置检查。
