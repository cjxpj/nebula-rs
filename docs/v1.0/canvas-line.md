# $绘制.线$ — 绘制线段

导入：`#引入=@画布` | 标准库函数。

- **签名**：`$绘制.线 [handle] [x1] [y1] [x2] [y2] [color]$`
- **参数**：画布句柄、起点坐标、终点坐标、可选的颜色
- **返回值**：无

算法：使用 Bresenham 直线算法绘制线段，画笔大小影响线宽。

```
$绘制.线 %_% 0 0 200 200$
```